-- script displays all records with score greater than 10
-- display is ordered by score in descending order
-- database is passed as mysql argument
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;

